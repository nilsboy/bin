#!/usr/bin/env bash

# Add, commit and push in one step

source bash-helpers

comment="$@"

git add -A .
if [[ ! "$comment" ]] ; then
  git commit -v
else
  git commit -v -m "$(cat ~/.gitmessage | grep -v '#')$comment"
fi
git push
