#!/bin/bash 

# Find files excluding dotfiles optional matching several strings

if [[ $abs ]] ; then
    abs=$(abs)
fi

find -H $abs* -mount \
    | perl -ne 'print if ! m#/\.#' \
    | grep-and -e $@
