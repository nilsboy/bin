# Display or search pstree, exclude current process

search=${@:-,$PPID}

pstree -apl \
    | perl -ne '$x = "xxSKIPme"; print if $_ !~ /[\|`]\-\{[\w-_]+},\d+$|less.+\+\/'$1'|$x/' \
    | less "+/$search"
